<!-- Attempted @m3e/react (v2.5.2). Install succeeded, but rendering/import in Vitest+jsdom fails due to `document.adoptedStyleSheets is not iterable` in @m3e internals. Using local MD3 CSS-token wrapper primitives for stable test/runtime baseline. -->

# MD3 wrapper primitives

Local wrappers provide MD3-style baseline components with CSS custom property tokens.
